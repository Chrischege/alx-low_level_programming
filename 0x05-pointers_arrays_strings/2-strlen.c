#include "main.h"
/**
 * _strlen - calculates lenght of a string
 * @s: char target
 * Return: nothing
 */

int _strlen(char *s)
{
	int cnt, in;

	in = 0;

	for (count = 0; s[count] != '\0'; count++)
	inc++;

return (inc);
}
